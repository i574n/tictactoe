open solid


let base_button (props : mut solid_props) =
    button
        fun props' =>
            {props' with
                Size = Some "xs"
                JustifyContent = Some "left"
                Compact = Some true
                ColorScheme = Some "neutral"
            } |> merge_props *props
        (props.Children /?? fun () => ;[])
