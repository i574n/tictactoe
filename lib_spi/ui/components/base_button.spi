open solid
open real_solid


inl base_button (props : mut_props) =
    button
        fun props' =>
            {props' with
                Size = Some "xs"
                JustifyContent = Some "left"
                Compact = Some true
                ColorScheme = Some "neutral"
            } |> merge_props *props
        fun () => ;[]
