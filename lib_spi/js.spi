nominal any = $"obj"

nominal js_function_ = $"JS.Function"

inl to_any obj : any = $"!obj"

inl import name module =
    inl name : string = name
    inl module : string = module
    $"import !name !module" : js_function_
