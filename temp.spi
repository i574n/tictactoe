nominal _obj = $"obj"

union target =
    | Fsharp
    | Cython

inl get_current_target () = 
    Fsharp

inl inspect ~obj =
    match get_current_target () with
    | Fsharp => $"printfn $\"%A{!obj}\""
    | Cython => $"print(!obj)"

inl test b = assert b "test"
